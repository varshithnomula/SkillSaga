import { MessageSubscriberAuthTokenOut } from "../models/messageSubscriberAuthTokenOut";
import { SvixRequestContext } from "../request";
export interface MessageEventsSubscriptionCreateTokenOptions {
    idempotencyKey?: string;
}
export declare class MessageEventsSubscription {
    private readonly requestCtx;
    constructor(requestCtx: SvixRequestContext);
    createToken(appId: string, subscriptionId: string, options?: MessageEventsSubscriptionCreateTokenOptions): Promise<MessageSubscriberAuthTokenOut>;
}
