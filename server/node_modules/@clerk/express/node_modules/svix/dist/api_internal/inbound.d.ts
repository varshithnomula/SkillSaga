import { MessageOut } from "../models/messageOut";
import { RotatedUrlOut } from "../models/rotatedUrlOut";
import { SvixRequestContext } from "../request";
export interface InboundMsgOptions {
    eventType?: string;
    idempotencyKey?: string;
}
export interface InboundRotateUrlOptions {
    idempotencyKey?: string;
}
export declare class Inbound {
    private readonly requestCtx;
    constructor(requestCtx: SvixRequestContext);
    msg(appId: string, inboundToken: string, options?: InboundMsgOptions): Promise<MessageOut>;
    rotateUrl(appId: string, options?: InboundRotateUrlOptions): Promise<RotatedUrlOut>;
}
